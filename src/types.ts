export type RootStackParamList = {
  Home: undefined;
  ColorPicker: undefined;
};

export type Hsv = {
  h: number;
  s: number;
  v: number;
};

export type Hsl = {
  h: number;
  s: number;
  l: number;
};

export type Coordinates = {
  x: number;
  y: number;
};
